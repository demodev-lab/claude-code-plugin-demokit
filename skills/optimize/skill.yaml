name: optimize
argument-hint: "도메인명, 파일경로, all [--fix] (예: User, all --fix)"
agent: domain-expert
pdca-phase: analyze
task-template: "Optimize {feature}"
imports:
  - shared/spring-conventions.md
  - shared/jpa-patterns.md
