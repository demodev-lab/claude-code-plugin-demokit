{
  "name": "claude-code-plugin-be",
  "version": "1.2.3",
  "private": true,
  "scripts": {
    "build": "tsc",
    "prebuild": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "jest --watchman=false",
    "test:unit": "jest test/unit",
    "test:integration": "jest test/integration",
    "validate:plugin": "node scripts/validate-plugin.js",
    "validate:hooks": "node scripts/validate-hooks.js",
    "sync:graph-index": "node scripts/generate-graph-index.js",
    "check:graph-index": "node scripts/generate-graph-index.js --check"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "typescript": "^5.7.0"
  }
}
